package com.coderank.model.enums;

/**
 * Supported programming languages for code execution.
 * Only Java is supported in Phase 1. Add new entries here (e.g. PYTHON, CPP)
 * when extending language support -- the sandbox executor will need a
 * corresponding compiler/runner implementation for each new language.
 */
public enum Language {
    JAVA
}
